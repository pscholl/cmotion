<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.uni_freiburg.es.sensorrecordingtool">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
    <uses-feature android:name="android.hardware.type.watch" android:required="true"/>
    -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <receiver android:name=".RecorderCommands">
            <intent-filter>
                <action android:name="senserec" />
                <action android:name="senserec_cancel" />
                <action android:name="senserec_ready" />
                <action android:name="senserec_steady" />
            </intent-filter>
        </receiver>

        <receiver android:name=".autodiscovery.DiscoveryReceiver" android:enabled="true">
            <intent-filter>
                <action android:name="senserec_discovery" />
            </intent-filter>
        </receiver>


        <service android:name=".Recorder"></service>
        <activity android:name=".PermissionHelperActivity" />
    </application>

</manifest>
